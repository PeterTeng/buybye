%main.single-main
  .login-panel
    .login-no-account
      %p アカウントをお持ちでない方はこちら
      = link_to "新規会員登録", new_user_path
    = form_for (:session), url: login_path, html: { role: 'form' }, novalidate: :novalidate do |f|
      = f.error_messages!
      .login-form-inner
        = render 'registrate/form', f: f
        %button{ "type" => "submit",  class: "login-submit btn-default btn-red"} ログイン
        -# %button
        -#   = f.submit "ログイン", class: "login-submit btn-default btn-red"
        = link_to "パスワードをお忘れの方", ""
